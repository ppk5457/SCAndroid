<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.edu.sc.train">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".IndexActivity"></activity>
        <activity android:name=".ViewPagerActivity3"></activity>
        <activity android:name=".fragment.MainFragmentActivity" />
        <activity android:name=".RecyclerActivity4" />
        <activity android:name=".RecyclerActivity3" />
        <activity android:name=".RecyclerActivity2" />
        <activity android:name=".RecyclerActivity" />
        <activity android:name=".ListViewShowActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity3" />
        <activity android:name=".DialogListActivity3"></activity>
        <activity android:name=".MainActivity2"></activity>
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>